# =============================================================================
# VectorForge MCP Server - Environment Configuration Template
# =============================================================================
# Copy this file to .env and customize the values for your environment.
# All settings have sensible defaults if not specified.

# =============================================================================
# Server Metadata
# =============================================================================

# Display name for the MCP server
VF_SERVER_NAME="VectorForge MCP Server"

# Short description of the MCP server reported to MCP clients
VF_SERVER_DESCRIPTION="Model Context Protocol server for VectorForge vector database"

# =============================================================================
# VectorForge API Connection
# =============================================================================

# Base URL of the VectorForge REST API
# Update if running VectorForge on a different host/port
VECTORFORGE_API_BASE_URL="http://localhost:3001"

# =============================================================================
# Collection Defaults
# =============================================================================

# Default collection used when no collection_name argument is provided to a tool
VF_DEFAULT_COLLECTION_NAME="vectorforge"

# Default number of search results returned by search_documents
# Range: 1-100, adjust based on your use case
VF_DEFAULT_TOP_K=10

# =============================================================================
# Logging Configuration
# =============================================================================

# Logging verbosity level for the MCP server process
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Use DEBUG for development, INFO or WARNING for production
VF_LOG_LEVEL=INFO

# Log record format string passed to logging.basicConfig
# Default format includes timestamp, logger name, level, and message
VF_LOG_FORMAT="%(asctime)s - %(name)s - %(levelname)s - %(message)s"
