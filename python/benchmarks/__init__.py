"""VectorForge Benchmarking Suite

This package contains comprehensive performance benchmarks for VectorForge,
testing search, indexing, file processing, persistence, and scaling behavior.

Run benchmarks with:
    pytest benchmarks/ --benchmark-only

Compare with baseline:
    pytest benchmarks/ --benchmark-compare=baseline

Save new baseline:
    pytest benchmarks/ --benchmark-save=baseline
"""
